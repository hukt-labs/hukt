{
  "name": "@hukt-labs/hukt-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "HUKT TypeScript SDK: offchain ExtraAccountMetaList resolution, composability adapter, hook builder, one-line resolver, and CLI for Solana Token-2022 transfer hooks.",
  "license": "MIT",
  "workspaces": [
    "account-resolver",
    "composability-adapter",
    "hook-builder",
    "resolver",
    "cli"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=18"
  }
}
