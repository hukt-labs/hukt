{
  "name": "@hukt/hook-builder",
  "version": "0.1.0",
  "private": true,
  "description": "No-code composition of verified presets into a single Token-2022 transfer-hook deployment spec.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.9"
  },
  "license": "MIT"
}
