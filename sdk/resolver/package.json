{
  "name": "@hukt/resolver",
  "version": "0.1.0",
  "private": true,
  "description": "One-line integration for Token-2022 transfer-hook tokens: hukt.resolve(mint) returns the extra accounts a transfer needs.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "license": "MIT"
}
