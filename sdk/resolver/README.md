# @hukt/resolver

One-line integration for Token-2022 transfer-hook tokens. `hukt.resolve(mint)` returns the extra accounts a transfer needs so wallets, DEXs, and lending protocols can build a valid transfer instruction.
