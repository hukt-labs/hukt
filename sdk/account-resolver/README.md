# @hukt/account-resolver

Automatic offchain resolution of a Token-2022 transfer hook's ExtraAccountMetaList, following the spl-transfer-hook-interface spec so a wallet or DEX can build a valid transfer in one call.
