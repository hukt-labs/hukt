# @hukt/hook-registry

On-chain registry of deployed transfer hooks plus safety attestation: attestors scan hooks for transfer-blocking and fund-draining patterns and stake a bond that is slashed if a malicious hook is passed as safe.
